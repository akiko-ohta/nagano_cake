<h2>注文履歴一覧</h2>
<% @orders.each do |order| %>
 <%= order.created_at.strftime('%Y/%m/%d') %>
 <%= order.shipping_postal_code %>
 <%= order.shipping_address %>
 <%= order.shipping_name %>
 <% order.order_details.each do |order_detail| %>
   <%= order_detail.item.name %>
 <% end %>
<%= order.amount_bill %>
<%= link_to order_path(order.id), class: 'btn text-decoration-none text-light btn-primary btn-sm' do %>
  <span>表示する</span>
<% end %>
<% end %>